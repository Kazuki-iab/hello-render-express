{
  "name": "hello-render-express",
  "version": "1.0.0",
  "description": "Minimal Express app for Render deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
